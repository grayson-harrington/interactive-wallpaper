# images

Your own pictures, read live by the server (no rebuild needed). Contents are gitignored;
only the folders are tracked.

- `wallpapers/`: photos for **Traditional Wallpaper** (hourly slideshow)
- `voronoi/`: source pictures for **Voronoi Images**

Supported: JPEG, PNG, GIF, WebP, AVIF, HEIC, BMP.
